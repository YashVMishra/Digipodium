// Create an array of names and filter names with length greater than 5.

const names=['Yash', 'Akash', 'Yashika', 'Virendra', 'Vibhash', 'Utkarsh', 'Vaibhav'];

const newNames=names.filter((p) => {return p.length>5})

console.log(newNames);